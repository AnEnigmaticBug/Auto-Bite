<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/exo</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:statusBarColor">@color/gry01</item>
        <item name="android:windowBackground">@color/wht01</item>
        <item name="android:colorEdgeEffect">@color/gry03</item>
        <item name="android:textCursorDrawable">@drawable/sh_cursor</item>
    </style>

</resources>
